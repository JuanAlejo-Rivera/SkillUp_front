import {RiseLoader} from "react-spinners";

const ReactSpinner = () => (
  <div className="flex justify-center items-center h-screen">
    <RiseLoader color="#3498db" size={20} />
  </div>
);

export default ReactSpinner;